#pragma once
#include <string>
#include <iostream>
#include <ctime>
using namespace std;
bool fails(int days, int month);
bool visokos(int years);
int SNOD(int years, int days, int month);
string nameMonth(int month);
string svyata(int numDay,bool check);
//int different(int days, int month, int years);

